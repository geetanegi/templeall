import * as React from 'react';
import PostSubmissionForm from '../components/ClientInquiryForm/postSubmissionForm';

function PostSubmissionIntakeFormPage(): React.JSX.Element {
    return <PostSubmissionForm />;
}
export default PostSubmissionIntakeFormPage;
